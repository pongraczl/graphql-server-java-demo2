type Query {
    humanById(id: ID!): Human
    allHumans: [Human]!
}

type Human {
    id: ID
    name: String
    profession: String
    lifeStatus: LifeStatus!
}

enum LifeStatus {
    ALIVE
    LIVING_DEAD
    REALLY_DEAD
}
